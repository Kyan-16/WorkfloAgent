# Agent Template - Python 依赖

# ---------- 核心框架 ----------
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0

# ---------- LLM SDK ----------
dashscope>=1.14.0
openai>=1.6.0
langgraph>=0.2.0

# ---------- RAG ----------
qdrant-client>=1.7.0

# ---------- Memory ----------
redis>=5.0.0

# ---------- 配置 ----------
pyyaml>=6.0

# ---------- HTTP ----------
httpx>=0.25.0
aiohttp>=3.9.0

# ---------- 工具 ----------
python-dotenv>=1.0.0

# ---------- 数据库 ----------
sqlalchemy>=2.0.0
pymysql>=1.0.0   # MySQL 驱动（如使用 MySQL 需安装）

# ---------- 飞书集成（可选）----------
# cryptography>=41.0.0   # Feishu 消息解密需要

# ---------- 认证（JWT + 密码哈希）----------
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# ---------- 监控（Prometheus 指标采集）----------
prometheus-fastapi-instrumentator>=7.0.0
prometheus-client>=0.21.0

# ---------- 开发 / 测试 ----------
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
coverage>=7.4.0

